{
  "name": "nky-delivery-bigcommerce",
  "version": "1.0.0",
  "description": "BigCommerce 配送/データAPI (Cloudflare Worker / Hono)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --env dev",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:prod": "wrangler deploy --env prod",
    "deploy": "wrangler deploy --env dev",
    "typecheck": "tsc --noEmit",
    "typegen": "wrangler types",
    "seed": "node scripts/seed-test-products.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "cf:dev": "wrangler dev --env dev",
    "cf:deploy": "wrangler deploy --env dev",
    "cf:typecheck": "tsc --noEmit",
    "cf:typegen": "wrangler types"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shintaro-i/nky-delivery-bigcommerce.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/shintaro-i/nky-delivery-bigcommerce/issues"
  },
  "homepage": "https://github.com/shintaro-i/nky-delivery-bigcommerce#readme",
  "dependencies": {
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260901.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.127.1",
    "axios": "^1.18.0",
    "csv-parse": "^7.0.0",
    "dotenv": "^17.4.2"
  }
}
